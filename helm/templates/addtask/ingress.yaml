apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: add-task-ingress
    
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Value.addtask.host }}
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: add-task
            port: 
              number: 80
